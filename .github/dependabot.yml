version: 2
updates:
  # npm dependencies (pnpm monorepo)
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
    reviewers:
      - "dominikhorn93"
      - "emaarco"
    groups:
      npm-dependencies:
        patterns:
          - "*"
        update-types:
          - "patch"
          - "minor"
          - "major"

  # Gradle / Kotlin plugins
  - package-ecosystem: "gradle"
    directory: "/engine-plugins"
    schedule:
      interval: "weekly"
    reviewers:
      - "dominikhorn93"
      - "emaarco"
    groups:
      gradle-dependencies:
        patterns:
          - "*"
        update-types:
          - "patch"
          - "minor"
          - "major"

  # GitHub Actions
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    reviewers:
      - "dominikhorn93"
      - "emaarco"
    groups:
      github-actions:
        patterns:
          - "*"
        update-types:
          - "patch"
          - "minor"
          - "major"

  # Docker images — playground: compose stack, Fly variants, engine
  - package-ecosystem: "docker"
    directories:
      - "/playground/docker"
      - "/playground/docker/otel"
      - "/playground/docker/prometheus"
      - "/playground/docker/grafana"
      - "/playground/cibseven-example"
    schedule:
      interval: "weekly"
    reviewers:
      - "dominikhorn93"
      - "emaarco"
    groups:
      docker-dependencies:
        patterns:
          - "*"
        update-types:
          - "patch"
          - "minor"
          - "major"

  # Docker image — root Dockerfile (gateway), keeps the pinned base digest current
  - package-ecosystem: "docker"
    directory: "/"
    schedule:
      interval: "weekly"
    reviewers:
      - "dominikhorn93"
      - "emaarco"
    groups:
      docker-root:
        patterns:
          - "*"
        update-types:
          - "patch"
          - "minor"
          - "major"
