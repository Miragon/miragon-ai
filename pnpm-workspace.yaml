packages:
  - "apps/*"
  - "packages/*"
  - "examples/*"
  - "docs"

# Interim until the toolkit ships the full dual-protocol widget contract
# (planned 0.8.0): without these _meta keys ext-apps hosts (Claude Desktop /
# claude.ai) don't recognise widget tools and every widget hangs on its
# loading skeleton. Remove together with the toolkit version bump.
patchedDependencies:
  "@miragon/mcp-toolkit-core@0.7.2": patches/@miragon__mcp-toolkit-core@0.7.2.patch
