kotlin.code.style=official

# Published artefact version for all engine-plugin modules. Release by pushing
# a matching `engine-plugins-v<version>` git tag (see
# .github/workflows/engine-plugins-publish.yml).
version=0.1.0
