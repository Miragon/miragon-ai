apiVersion: 1

# PROMETHEUS_URL comes from the runtime environment so the same provisioning
# works in Compose (http://prometheus:9090) and on Fly.io (…internal:9090).
datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus
    access: proxy
    url: $PROMETHEUS_URL
    isDefault: true
    editable: false
