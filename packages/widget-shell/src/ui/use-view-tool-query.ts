/**
 * Re-exports the toolkit's structured-first self-fetch hook for `*_show_*`
 * widget tools (`@miragon/mcp-toolkit-ui/hooks`, 0.4.0). Kept as a local module
 * so the `/widgets` barrel keeps a stable path.
 */
export { useViewToolQuery, type UseViewToolQueryOptions } from "@miragon/mcp-toolkit-ui/hooks"
