shamefully-hoist=false
@miragon:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
save-exact=true
# Run package scripts through pnpm's POSIX shell emulator so the
# `${VAR:-default}` idiom (root dev script, server --port) works on Windows.
shell-emulator=true
